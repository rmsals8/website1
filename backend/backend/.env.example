# .env 파일 형식 예시 (실제 값은 .env에 넣고 커밋하지 마세요)
ConnectionStrings__Default=Server=<host>;Port=<port>;Database=<db_name>;User=<user>;Password=<password>;

# 로컬 개발용 포트 (호스팅 환경에서는 Cloudtype이 자체적으로 포트를 주입하므로 이 값은 무시됨/불필요)
ASPNETCORE_URLS=http://localhost:5299
